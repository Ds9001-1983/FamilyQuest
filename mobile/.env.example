# Optional overrides — most config lives in app.json "extra".
# For dev you typically run the API on localhost:5000.
# Point this at your deployed backend for TestFlight/production builds.
EXPO_PUBLIC_API_URL=http://localhost:5000
